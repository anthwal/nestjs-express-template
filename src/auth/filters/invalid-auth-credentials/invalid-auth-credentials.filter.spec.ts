import { InvalidAuthCredentialsFilter } from './invalid-auth-credentials.filter';

describe('InvalidAuthCredentialsFilter', () => {
  it('should be defined', () => {
    expect(new InvalidAuthCredentialsFilter()).toBeDefined();
  });
});
